#include <becam.h>

static DeviceInfo getDeviceInfoListItem(DeviceInfo* list, size_t index) {
    return list[index];
}

static VideoFrameInfo getFrameInfoListItem(VideoFrameInfo* list, size_t index) {
    return list[index];
}